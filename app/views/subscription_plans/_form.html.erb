<%= form_for([@newspaper, @plan]) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= render :partial => "inner_form", :locals => { :f => f, :plan => @plan } %>
  <br>
  <%= f.submit class: "btn" %>
  <%= link_to "Cancel", :back, class: "btn" %>
<% end %>